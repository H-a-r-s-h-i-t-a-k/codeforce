#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{

    int n;
    cin >> n;
    bool flag = false;
    string s = "";
    if (i >= 1)
        cin >> s[0];
    for (int i = 1; i < n; i++)
    {
        cin >> s[i];
        if (s[i] != s[i - 1])
            flag = true;
    }

    if (flag)
        {
            
        }

            return 0;
}