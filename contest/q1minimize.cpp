#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ll k;
    cin >> k;
    while (k--)
    {
        ll a, b;
        cin >> a >> b;
        cout << b - a << endl;
    }
    return 0;
}