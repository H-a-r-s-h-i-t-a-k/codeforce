#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long t, n;
    vector<int> arr;
    cin >> t;
    while (t--)
    {
        cin >> n;
        int v;
        if (n = 1)
        {
            cin >> v;
            cout << v << endl;
        }
        for (int i = 0; i < n; i++)
        {

            cin >> v;
            arr.push_back(v);
        }

        cout <<  << endl;
    }
    return 0;
}